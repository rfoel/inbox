---
import Layout from '../layouts/Layout.astro'
import { Recipients } from '../components/Recipients'
import { Main } from '../components/Main'

if (!Astro.cookies.has('inbox_jwt')) {
  return Astro.redirect('/authenticate')
}
---

<Layout title="Inbox">
  <Main client:load />
</Layout>
